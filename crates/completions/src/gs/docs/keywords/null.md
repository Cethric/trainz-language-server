- null is an uninitialised expression.
- Every reference that is not assigned to an object has the value null.
- Assigning null to an existing object will remove that reference to that object. When no references remain, the object
  will be deleted.
