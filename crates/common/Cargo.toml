[package]
name = "gs-common"
version = "0.1.0"
edition.workspace = true
build = "build.rs"

[dependencies]
shadow-rs = { workspace = true }
log = { workspace = true }
env_logger = { workspace = true }

[build-dependencies]
shadow-rs = { version = "1.7" }